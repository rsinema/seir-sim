# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 2. Allow Git to look into specific subdirectories (don't ignore the folders)
!out/**/

# 3. Ignore all PNGs by default within the out directory
out/**/*.png
out/**/*.json

# 4. Re-include the specific PNGs you want to track
# This tracks the averages image in the experiment folder
!out/*/*.png

# This tracks the degree distribution image in each run folder
!out/*/run_*/*.png

# 5. Specifically ignore the dense image directories
out/*/run_*/graph_images/
out/*/run_*/graph_state/